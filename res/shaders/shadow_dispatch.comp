#version 450

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;


layout(binding=5, std140) buffer Counters {
	uint shadowCounter;
	uint reflrectionCounter;
};

layout(binding=6, std430) buffer DispatchIndirect {
	uvec3 dispatchIndirect[];
};

//Handling intersection

void main() {
	dispatchIndirect[0] = uvec3(uint(ceil(float(shadowCounter) / 64)), 1, 1);
}